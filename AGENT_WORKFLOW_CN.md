# 通用多编程智能体项目开发工作流

本文件用于初始化由一个或多个编程智能体参与开发的软件项目。

该工作流与具体工具无关，适用于 Codex、Claude Code、基于 DeepSeek 的编程智能体及其他类似工具。

它的目标是以较低的上下文和维护成本，持续维护：

- 经确认的项目意图；
- 当前任务状态；
- 可复用的重要决策与经验；
- 客观验证证据；

同时避免重复记录代码、测试、配置和 Git 中已经存在的信息。

---

## 1. 核心文件

在项目根目录长期维护四个文件：

```text
AGENTS.md
PROJECT.md
TASK.md
DEVLOG.md
```

每个文件只承担一种职责：

| 文件 | 唯一回答的问题 |
|---|---|
| `AGENTS.md` | 编程智能体在本仓库中必须怎样工作？ |
| `PROJECT.md` | 项目必须实现什么，当前项目的系统级事实是什么？ |
| `TASK.md` | 当前任务是什么，进行到哪里？ |
| `DEVLOG.md` | 哪些历史决策、失败方案和里程碑仍会影响后续开发？ |

其他信息继续由现有工程载体负责：

| 信息 | 唯一事实源 |
|---|---|
| 精确代码变化 | Git diff 和 commit 历史 |
| 正确性 | 测试、CI、类型检查、lint 和运行检查 |
| 依赖与版本 | package 和 lock 文件 |
| API 或 schema 细节 | 代码、schema 文件和生成文档 |
| 普通任务历史 | Git commit 或 issue tracker |

除非本工作流的扩展规则明确要求，否则不要再创建 `MEMORY.md`、
`STATUS.md`、`PROGRESS.md`、`HANDOFF.md`、`CONTEXT.md` 或 `PLAN.md`
等管理文件。

---

## 2. 初始化

将本工作流应用到新仓库或现有仓库时：

1. 检查用户需求、README、仓库结构、配置文件、package 文件、测试和当前 Git 状态。
2. 只使用经过验证的事实创建或更新四个核心文件。
3. 将用户需求整理为 `PROJECT.md` 中简洁、可审查的需求基线，不要复制完整聊天记录。
4. 对于新项目或需求明显不完整的项目，应在大规模开发前让用户确认需求基线。
5. 无法确认的信息写为 `待确认`，不得自行编造。
6. 仅进行文档初始化时，不修改业务代码。
7. 已经能够从代码、配置、测试或 Git 中可靠获得的信息，不在 Markdown 中重复维护。

---

## 3. `AGENTS.md`

### 作用

`AGENTS.md` 是所有编程智能体进入仓库时应首先读取的简短入口文件。

它只包含：

- 必要的阅读顺序；
- 已验证的开发命令；
- 仓库级约束；
- 审查和完成规则；
- 指向项目状态和任务状态的入口。

不得将其扩展为项目百科全书。

### 模板

````markdown
# AGENTS.md

## 开始前阅读

修改代码前：

1. 阅读 `PROJECT.md`。
2. 阅读 `TASK.md`。
3. 仅检索 `DEVLOG.md` 中与当前组件或风险相关的条目。
4. 检查相关代码、测试和当前 Git diff。

不要依赖之前的聊天记录。
如果文档与已经验证的仓库行为冲突，应先调查原因，并在当前任务范围内修正过时的信息源。

## 命令

```bash
# 安装或初始化
[已验证命令或待确认]

# 运行
[已验证命令或待确认]

# 测试
[已验证命令或待确认]

# 完整检查
[已验证命令或待确认]
```

## 规则

- 遵守 `PROJECT.md` 中已经确认的需求和约束。
- 严格遵守 `TASK.md` 中的范围和验收标准。
- 遵循仓库现有代码和测试约定。
- 只进行满足任务所需的最小且完整的改动。
- 不进行无关重构。
- 不得静默修改公共接口、schema、依赖、架构或已经批准的需求。
- 不得编造仓库事实，也不得声称未运行的检查已经通过。
- 行为发生变化时，增加或更新测试。
- 不得泄露或提交凭据、密钥、私钥和隐私数据。
- 精确修改历史由 Git 保存。
- 保持管理文件最新、简洁且互不重复。

## 工作循环

1. 根据真实仓库状态核对任务。
2. 验收标准不清晰时，先明确再实现。
3. 增量实现。
4. 运行相关检查。
5. 审查 diff 中的回归、范围扩张和意外修改。
6. 停止工作前更新 `TASK.md`。
7. 只有满足更新条件时才修改 `PROJECT.md` 或 `DEVLOG.md`。

## 审查策略

- **低风险：** 自审并运行相关自动检查。
- **中风险：** 明确进行一次代码差异审查（diff review），并运行相关检查。
- **高风险：** 运行相关检查，并由另一个智能体或在独立的新会话中进行审查。

高风险工作包括安全敏感代码、认证、权限、数据迁移、公共 API、
核心 schema、计费、并发和大范围架构重构。

## 完成标准

- `TASK.md` 中的验收条件已经满足。
- 已运行相关检查，或明确记录无法运行的原因。
- 最终 diff 不包含无关修改。
- 已完成对应风险等级要求的审查。
- `TASK.md` 明确写明任务完成，或保留一个下一步。
````

### 维护规则

只在以下情况更新 `AGENTS.md`：

- 稳定的仓库命令发生变化；
- 仓库级约束发生变化；
- 智能体反复出现同类错误，说明缺少必要规则；
- 审查或完成策略发生变化。

删除过时、重复或显而易见的指令。能够自动检查的规则优先交给测试或 CI。

建议长度：约 60–120 行。

---

## 4. `PROJECT.md`

### 作用

`PROJECT.md` 是项目级契约与系统概览。

它包含：

1. 经确认的需求基线；
2. 受控的需求变更记录；
3. 项目级能力清单；
4. 当前系统级结构与约束。

需求基线用于防止项目漂移，但并非永久不可修改。任何需求变化必须经过明确批准，并留下 amendment 记录。

### 模板

````markdown
# PROJECT.md

## 需求基线

**项目名称：** [名称]

**问题：**  
[项目必须解决什么真实问题？]

**目标结果：**  
[项目成功时应交付什么成果？]

**主要用户或使用场景：**  
[谁会在什么场景下使用？不适用时删除。]

**核心流程：**  
[用一行描述端到端流程。]

### 必备能力

- **R1：** [核心能力]
- **R2：** [核心能力]
- **R3：** [核心能力]

### 非目标

- **N1：** [明确不做什么]
- **N2：** [明确不做什么]

### 项目级成功标准

- **S1：** [可观察、可判断的项目级标准]
- **S2：** [可观察、可判断的项目级标准]

### 不可突破的约束

- **C1：** [隐私、兼容性、成本、质量、部署或其他约束]
- **C2：** [约束]

> 编程智能体不得静默改写本需求基线。
> 任何变化都必须经过用户明确批准，并增加需求变更条目。

## 需求变更

- **A1 — YYYY-MM-DD：** [批准的变化、受影响的 ID 和原因]
- 无

## 能力清单

| ID | 能力 | 状态 | 证据 |
|---|---|---|---|
| R1 | [能力] | 未开始 / 部分完成 / 已验证 | [测试、路径、版本或无] |
| R2 | [能力] | 未开始 / 部分完成 / 已验证 | [证据或无] |

只有存在客观证据时，才能将能力标记为“已验证”。

## 当前系统概览

**阶段：** [规划 / 原型 / 实现 / 集成 / 稳定化]

**当前流程：**  
[当前实际的数据流或控制流。]

**主要组件：**

- `[路径或组件]`：[职责和当前系统级状态]
- `[路径或组件]`：[职责和当前系统级状态]

**重要接口或 schema：**

- `[路径或接口]`：[用途]
- `[路径或接口]`：[用途]

**当前生效的技术选型：**

- [当前有效选择]
- [当前有效选择]

**项目级风险或开放问题：**

- [影响多个任务或项目方向的问题]
- 无
````

### 可追踪性

每个实现任务必须关联一个或多个需求、成功标准或约束 ID：

```text
关联项目项：R2、S1、C1
```

无法关联需求基线的任务，应明确说明它属于维护、技术债务，或经过批准的需求变化。

### 维护规则

在以下情况更新 `PROJECT.md`：

- 用户批准需求变化；
- 某项能力获得新的客观验证证据；
- 系统流程或主要组件边界变化；
- 公共接口或核心 schema 变化；
- 重要技术选择正式生效；
- 项目级风险出现或解决；
- 项目进入新阶段。

不要加入任务过程、详细文件修改、普通 bug 历史和原始讨论。当前状态发生变化时，直接替换旧描述，不要不断追加旧版本。

建议长度：约 120–250 行。

---

## 5. `TASK.md`

### 作用

`TASK.md` 是当前任务的工作契约和交接状态。

它负责把项目级意图连接到具体实现和验证证据。

### 模板

````markdown
# TASK.md

## 任务信息

**任务：** T-[编号] — [简短名称]  
**关联项目项：** [R#、S#、C#，或维护工作说明]  
**风险：** 低 / 中 / 高  
**状态：** 待开始 / 进行中 / 阻塞 / 审查中 / 已完成

## 目标

[一个具体、可观察的结果。]

## 边界

**任务范围内：**

- [必须完成]
- [必须完成]

**任务范围外：**

- [明确排除]
- [明确排除]

## 验收条件

- [ ] [可观察的完成条件]
- [ ] [可观察的完成条件]
- [ ] 相关自动检查通过。
- [ ] 未引入无关修改。
- [ ] 已完成当前风险等级要求的审查。

## 计划

1. [步骤]
2. [步骤]
3. [步骤]

简单任务删除本节。

## 当前状态

**已完成：**

- [有意义的完成结果]
- 无

**进行中：**

- [尚未完成的工作]
- 无

**下一步：**

- [下一位智能体可以立即执行的一个明确动作]

**阻塞项：**

- [阻塞问题以及如何解除]
- 无

## 验证

- `[命令或人工检查]` → 通过 / 失败 / 未运行
- `[命令或人工检查]` → 通过 / 失败 / 未运行

## 任务备注

- [仅记录继续本任务所必需的决定、约束、失败尝试或事实。]
- 不需要时删除本节。
````

### 任务设计规则

一个任务应该：

- 交付一个完整、连贯的结果；
- 具有可观察的验收条件；
- 足够小，能够由一个主要智能体负责；
- 避免无关重构；
- 结束时让仓库处于连贯状态；
- 关联项目需求，或明确说明维护原因。

如果任务包含可以分别成功或失败的独立交付物，应拆分任务。

### 维护规则

在以下时间更新 `TASK.md`：

- 完成一个有意义的工作单元后；
- 范围或验收标准发生变化时；
- 阻塞或失败方案影响后续工作时；
- 运行验证后；
- 切换智能体或结束会话前。

不要记录每条命令、每次编辑和完整思考过程。

建议长度：约 50–120 行。

---

## 6. `DEVLOG.md`

### 作用

`DEVLOG.md` 保存无法仅通过 Git 可靠恢复的历史上下文。

它是经过筛选的决策与经验索引，不是逐操作开发日志。

只记录：

- 重要里程碑；
- 跨任务决策及理由；
- 后续智能体可能重复的失败方案；
- 难以从最终代码看出的根因与可复用经验；
- 项目方向变化。

### 模板

````markdown
# DEVLOG.md

## 使用方式

按需求 ID、组件、决策或错误关键词检索本文件。
默认不要完整读取整个文件。

不要记录普通编辑、完整日志、代码 diff 或可以直接从 Git 恢复的事实。

## 条目

### D-[编号] — YYYY-MM-DD — [决策、里程碑或经验]

- **类型：** 决策 / 里程碑 / 失败方案 / 根因与经验
- **关联：** [R#、任务 ID、组件、issue 或无]
- **背景：** [简短问题或触发条件]
- **结果：** [最终形成了什么]
- **理由或经验：** [为什么未来工作需要知道]
- **避免重复：** [不应再次尝试的路径，或无]
- **证据：** [commit、测试、issue、benchmark 或路径]
````

### 记录条件

仅当满足以下至少一项时，才增加条目：

- 否则未来智能体可能重复一个代价较高的错误；
- 某项决定会约束多个后续任务；
- 项目完成了有意义的能力里程碑；
- 根因无法从最终代码直接看出；
- 项目方向或需求解释发生变化。

不要记录普通实现进度、小 bug、格式调整，以及 commit message 和 diff 已经能够清楚表达的信息。

### 长度控制

`DEVLOG.md` 变长时：

1. 将相关条目合并为阶段总结；
2. 删除已经不再影响未来工作的细节；
3. 保留证据链接或 commit 引用；
4. 保留近期和高影响条目；
5. 被删除的细节继续由 Git 保存。

建议活跃规模：约 20–40 条高价值记录。

---

## 7. 标准工作流

### 启动新项目

1. 初始化四个核心文件。
2. 让用户确认需求基线。
3. 在 `TASK.md` 中定义第一个任务。
4. 至少建立一个可执行的安装、测试或 smoke check 命令。
5. 开始增量实现。

### 开始新任务

1. 阅读 `AGENTS.md` 和 `PROJECT.md`。
2. 按相关需求 ID、组件和失败方案检索 `DEVLOG.md`。
3. 检查相关代码、测试、`git status` 和当前 diff。
4. 用一个明确范围的任务替换 `TASK.md`。
5. 关联项目 ID。
6. 定义可观察的验收条件。
7. 指定风险等级。
8. 仅在必要时增加简短计划。

替换已完成的 `TASK.md` 前，必须先将相关工作提交或以其他方式保存在 Git 历史中。

### 由另一个智能体继续

1. 阅读 `AGENTS.md`、`PROJECT.md` 和 `TASK.md`。
2. 检索相关 `DEVLOG.md` 条目。
3. 检查 Git 状态、近期提交、相关代码和测试。
4. 核对 `TASK.md` 与真实仓库状态是否一致。
5. 从“下一步”开始继续。
6. 依赖文档前先修正其中过时的信息。

没有证据时，不得直接接受上一智能体的完成声明。

### 停止或交接

停止工作前：

1. 尽量让仓库处于连贯状态；
2. 更新任务状态、验证结果、阻塞问题和明确的下一步；
3. 将有意义的工作提交或以其他方式保存在 Git 历史中；
4. 不创建额外交接文件；
5. 只有满足准入条件时才更新 `DEVLOG.md`。

### 审查和完成

1. 检查全部验收条件。
2. 运行相关自动和人工检查。
3. 审查最终 diff。
4. 执行对应风险等级的审查策略。
5. 项目级事实变化时更新 `PROJECT.md`。
6. 确有长期价值时增加 `DEVLOG.md` 条目。
7. 将任务标记为完成，并将相关工作保存在 Git 历史中。

---

## 8. 扩展规则

### 并行开发

根目录单一 `TASK.md` 只适合同时存在一个活动任务的情况。

两个或更多智能体并行开发时：

1. 每个任务使用独立分支或 worktree；
2. 创建独立任务文件：

```text
tasks/active/T-001-short-name.md
tasks/active/T-002-short-name.md
```

3. 每个任务只有一个主要负责人；
4. 除非明确协调，否则避免同时修改相同文件；
5. 明确任务依赖；
6. 相关工作保存在 Git 历史中后，删除或归档已完成的任务文件。

如果 Issue 管理系统已经承担任务管理职责，可以用它替代任务 Markdown 文件。不要在两个位置独立维护同一份任务状态。

### 复杂任务

只有在任务满足以下情况时，才创建任务专用执行计划：

- 跨多天或多个会话；
- 大范围架构重构；
- 不可逆迁移；
- 依赖多个里程碑；
- 无法通过简短验收条件充分描述。

示例：

```text
plans/T-014-exec-plan.md
```

`TASK.md` 应链接该计划，不要重复内容。普通任务不得创建独立计划文件。

---

## 9. 上下文与 Token 控制

默认阅读顺序：

```text
AGENTS.md
→ PROJECT.md
→ TASK.md 或分配的任务文件
→ 定向检索 DEVLOG.md
→ 当前任务相关代码和测试
```

规则：

- 除非任务需要全局探索，否则不要扫描整个仓库。
- 不要默认加载完整 `DEVLOG.md`。
- 每项事实只保存在一个唯一且明确的位置。
- 优先引用路径、符号、测试和 commit，不复制大段内容。
- 删除过时信息，而不是不断追加修正说明。
- 保存经确认的意图，不保存完整聊天记录。
- 可强制执行的要求交给自动检查。
- 精确历史交给 Git。
- Markdown 只保存无法从其他工程载体可靠恢复的意图、当前状态、理由和证据入口。
- 超过建议长度时，应主动压缩和清理管理文件。

---

## 10. 职责地图

| 问题 | 唯一事实源 |
|---|---|
| 项目最终必须实现什么？ | `PROJECT.md` 的需求基线 |
| 某项项目能力是否已经实现？ | `PROJECT.md` 的能力清单和证据 |
| 当前系统级设计是什么？ | `PROJECT.md` 的当前系统概览 |
| 编程智能体必须怎样工作？ | `AGENTS.md` |
| 当前应该做什么？ | `TASK.md` 或分配的任务文件 |
| 某项重要选择为什么这样做？ | `DEVLOG.md` |
| 哪些失败方案不应重复？ | `DEVLOG.md` |
| 具体修改了什么？ | Git diff 和历史 |
| 实现是否正确？ | 测试和自动检查 |

如果两个载体同时回答同一个问题，应删除其中的重复内容。
